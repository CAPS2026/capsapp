export default function ApplyVolunteerPage() {
  return (
    <main className="flex-1 flex flex-col items-center justify-center gap-3 px-6 text-center py-16">
      <h1 className="text-2xl font-extrabold" style={{ fontFamily: "var(--font-display)" }}>
        Get involved with CAPS
      </h1>
      <p className="text-ink-muted max-w-sm">
        The registration form (on-site help + homecare, docs/ui-flows.md §7)
        lands here in a later build slice. For now, contact CAPS directly.
      </p>
    </main>
  );
}
