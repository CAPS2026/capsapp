// Volunteer terms & conditions shown on the registration form.
//
// TODO(Paul): replace with the real text. Everything between the
// backticks renders as-is (blank lines separate paragraphs). Keep it
// plain text — no markup.
export const VOLUNTEER_TERMS = `
The full volunteer terms and conditions are being finalised by CAPS and
will appear here shortly.

By registering you agree to follow caretaker instructions at the shelter,
to handle animals only as directed, and to let CAPS know if your contact
or health details change.
`.trim();

export const VOLUNTEER_TERMS_READY = false; // flip to true once the real text is in
